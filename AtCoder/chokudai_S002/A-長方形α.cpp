#include <bits/stdc++.h>
#define int long long
using namespace std;
const int ios_init = (ios::sync_with_stdio(0), cin.tie(0), cout.tie(0), 0);
signed main(){
    int n; cin >> n;
    while(n--){
        int a, b;
        cin >> a >> b;
        cout << a*b << '\n';
    }
    return 0;
}